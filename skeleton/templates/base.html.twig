<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %} <link rel="stylesheet" href="{{ asset("css/style.css") }}">{% endblock %}
    </head>
    <body>
        <header>{% block header %}
                <h1>Le bon blog</h1>
                {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                    <p> <b>Bonjour {{ app.user.username }} </b> <a class="button-header" href=" {{ logout_path('main') }} ">Déconection</a></p>
                {% endif %}
                {% if is_granted("ROLE_ADMIN")  %}
                   <div id="but"> <a class="button-header"  href="{{ path("blog") }}">blog</a> <a  class="button-header" href="{{ path("show_user") }}">admin</a></div>
                {% endif %}

            {% endblock %}</header>
        {% block body %}{% endblock %}
        {% block javascripts %}
        {% endblock %}
    </body>
