{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset("css/blog.css") }}"> {% endblock %}
{% block body %}

    <h1>{{ article.title }}</h1>
    <h5>posté le {{ article.date }}</h5>

    <div id="contenu-article">
     <img id="image-article" src="{{ article.image }}">
    <div id="article-cont">{{ article.contenu }}</div>
    </div>
    <div id="main-comment">
        <div id="comment">
            <div id="comment-form">
            {{ form(form) }}
            </div>
           <hr>
            <div id="commentaire">
            {% for comment in comments %}
                {{ comment.user }} le {{ comment.date }}: <br>
                {{ comment }}<hr>
            {% endfor %}
        </div>
        </div>
    </div>


  {% endblock %}