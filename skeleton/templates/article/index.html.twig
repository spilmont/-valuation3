{% extends 'base.html.twig' %}

{% block title %}Article index{% endblock %}

{% block body %}
    <h2>panneau article</h2>

    <nav>
        <a href="{{ path('show_user') }}"> utilisateur</a>
        <a href="{{ path('comments_index') }}">Commentaire</a>
    </nav>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>titre</th>
                <th>description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.id }}</td>
                <td>{{ article.title }}</td>
                <td>{{ article.description }}</td>
                <td>
                    <span>{{ include('article/_delete_form.html.twig') }}</span>
                   <a href="{{ path('article_edit', {'id': article.id}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">auccun article trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   <div class="new"><a href="{{ path('article_new') }}">nouvel article</a></div>
{% endblock %}
